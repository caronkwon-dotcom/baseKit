CREATE TABLE BASEKIT_SCHEMA_MARKER (
    MARKER_ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    MARKER_CODE VARCHAR(50) NOT NULL UNIQUE,
    DESCRIPTION VARCHAR(500),
    CREATED_AT TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP
);

INSERT INTO BASEKIT_SCHEMA_MARKER (MARKER_CODE, DESCRIPTION)
VALUES ('BACKEND_FOUNDATION_V1', 'BaseKit backend foundation migration initialized');
