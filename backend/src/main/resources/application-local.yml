spring:
  datasource:
    url: jdbc:h2:file:./target/local-data/basekit;MODE=PostgreSQL;DATABASE_TO_UPPER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver
management:
  endpoint:
    health:
      show-details: always
